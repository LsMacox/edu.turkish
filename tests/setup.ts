// tests/setup.ts
import { afterAll, beforeAll } from 'vitest'

// сюда можно положить полифилы, mock fetch, таймауты и т.д.
beforeAll(() => {
  // например, зафиксировать таймеры или замокать console.error
})

afterAll(() => {
  // зачистка моков
})
