import withNuxt from './.nuxt/eslint.config.mjs'

// Recommended: use Nuxt's project-aware flat config and extend later if needed
export default withNuxt(
  // You can pass additional flat config objects here, e.g.
  // {
  //   rules: {
  //     // Example: allow console in dev
  //     // 'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off'
  //   }
  // }
)
