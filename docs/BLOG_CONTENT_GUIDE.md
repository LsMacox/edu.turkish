# 📝 Гайд по контенту блога

> **Коротко:** Весь контент — это JSON-массив блоков.
> Это позволяет нам делать **крутую верстку**, **вставлять виджеты** и **адаптировать дизайн**.

---

## � Hero-блок (Шапка)

Эти поля заполняются **отдельно от основного текста**, но они критически важны для первого впечатления.

| Поле             | Где отображается                | Пример                             |
| ---------------- | ------------------------------- | ---------------------------------- |
| **heroKicker**   | Маленькая плашка над заголовком | `Поступление 2025`                 |
| **title**        | Главный заголовок H1            | `Как поступить в Турцию?`          |
| **heroSubtitle** | Подзаголовок (лид) серым цветом | `Пошаговый гайд для абитуриентов.` |
| **heroLocation** | Локация (с иконкой 📍)          | `Стамбул, Турция`                  |

---

## �🧱 Основные блоки (Работают везде)

Эти блоки работают и в **Обычных статьях**, и в **Power Pages** (лендингах).

### 1️⃣ Заголовки (`heading`)

Создают структуру. В обычных статьях автоматически генерируют **Оглавление**.

```json
{
  "type": "heading",
  "level": 2, // 2 = H2 (Раздел), 3 = H3 (Подраздел)
  "text": "Заголовок раздела"
}
```

### 2️⃣ Текст (`paragraph`)

Обычный текст. Поддерживает HTML для форматирования.

**Можно использовать:**

- **Жирный**: `<b>текст</b>`
- _Курсив_: `<i>текст</i>`
- [Ссылка](https://google.com): `<a href="...">текст</a>`
- Перенос строки: `<br>`

```json
{
  "type": "paragraph",
  "text": "Текст с <b>жирным</b> выделением и <a href='#'>ссылкой</a>."
}
```

### 3️⃣ Списки (`list`) — 🔥 Фишка!

У нас есть 3 стиля списков.

**🔹 Обычный (`standard`)**
Просто точки или цифры.

```json
{
  "type": "list",
  "ordered": false, // true для цифр (1, 2, 3)
  "items": ["Пункт 1", "Пункт 2"]
}
```

**✅ Чек-лист (`checklist`)**
Список с зелеными галочками. Идеально для **списков документов**.

```json
{
  "type": "list",
  "style": "checklist",
  "items": ["Паспорт", "Аттестат", "Фото 3x4"]
}
```

**👣 Шаги (`steps`)**
Вертикальная линия с шагами. Идеально для **инструкций** ("Как поступить").

```json
{
  "type": "list",
  "ordered": true,
  "style": "steps",
  "items": ["<b>Шаг 1:</b> Оставьте заявку", "<b>Шаг 2:</b> Получите консультацию"]
}
```

### 4️⃣ Изображения (`image`)

Картинки можно растягивать.

```json
{
  "type": "image",
  "url": "/uploads/photo.jpg",
  "alt": "Описание для SEO",
  "caption": "Подпись под фото",
  "width": "standard" // Варианты ниже 👇
}
```

**Варианты ширины (`width`):**

- `standard`: По ширине текста (обычный).
- `wide`: Чуть шире текста (красиво!).
- `full`: **На весь экран** (для эффектных фото).

### 5️⃣ Цитаты (`quote`)

Выделенный блок с текстом.

```json
{
  "type": "quote",
  "text": "Главная мысль статьи.",
  "author": "Илон Маск" // Можно без автора
}
```

### 6️⃣ Разделители

- **Линия**: `{"type": "divider"}`
- **Пустое место**: `{"type": "spacer", "size": "md"}` (размеры: `sm`, `md`, `lg`, `xl`)

---

## 🎓 Программы/Направления (isProgram)

**Program** — это статья о направлении обучения (медицина, бизнес, инженерия и т.д.).
Включается флагом `isProgram: true` в seed/админке.

**Особенности:**

- Отображается на странице `/program/[slug]`
- Имеет сайдбар со списком всех направлений
- **Работают виджеты!** 🚀
- Категория должна иметь `isProgram: true`

---

## ⚡ Power Pages (Лендинги)

**Power Page** — это статья, которая выглядит как продающий лендинг.
Включается галочкой `isPowerPage: true` в админке.

**Отличия:**

- Нет боковой колонки (Sidebar).
- Нет оглавления.
- **Работают виджеты!** 🚀

### 🧩 Виджеты (Только Power Page)

Вставляются прямо в текст параграфа через специальные коды.

**🎓 Виджет ВУЗов**
Показывает красивые карточки университетов.

```json
{
  "type": "paragraph",
  "text": "Посмотрите лучшие варианты: {{WIDGET:UNIVERSITIES}}"
}
```

**💰 Виджет Цены**
Крупный блок с ценой.

```json
{
  "type": "paragraph",
  "text": "{{WIDGET:PRICE}} Обучение от $2,000 в год."
}
```

**❓ Виджет FAQ (Вопрос-Ответ)**
Раскрывающийся список вопросов (аккордеон).

```json
{
  "type": "faq",
  "items": [
    { "question": "Нужна ли виза?", "answer": "Да, студенческая." },
    { "question": "Сколько стоит?", "answer": "От $2000." }
  ]
}
```

**✅❌ Виджет Плюсы/Минусы (PROSCONS)**
Табы с причинами "за" и "против". Идеально для описания специальностей.

```json
{
  "type": "paragraph",
  "text": "{{WIDGET:PROSCONS:{\"title\":\"Специальность подойдёт если Вы:\",\"pros\":[{\"title\":\"Заголовок плюса\",\"description\":\"Описание плюса.\"}],\"cons\":[{\"title\":\"Заголовок минуса\",\"description\":\"Описание минуса.\"}]}}}"
}
```

**🖼️ Виджет Галерея изображений (GALLERY)**
Сетка 3x2 с кликабельными изображениями. При клике открывается lightbox с навигацией.

```json
{
  "type": "paragraph",
  "text": "{{WIDGET:GALLERY:{\"title\":\"Заголовок галереи\",\"images\":[{\"url\":\"https://cdn.edu-turkish.com/image1.jpg\",\"alt\":\"Описание 1\"},{\"url\":\"https://cdn.edu-turkish.com/image2.jpg\",\"alt\":\"Описание 2\"}]}}}"
}
```

**Структура JSON внутри виджета GALLERY:**

```json
{
  "title": "Заголовок галереи (опционально)",
  "images": [{ "url": "https://cdn.edu-turkish.com/image.jpg", "alt": "Описание (опционально)" }]
}
```

_Рекомендуется использовать 6 изображений для сетки 3x2. URL должен быть полным путём к CDN._

**Структура JSON внутри виджета:**

```json
{
  "title": "Заголовок виджета (опционально)",
  "pros": [
    { "title": "Плюс 1", "description": "Описание плюса" },
    { "title": "Плюс 2", "description": "Описание" }
  ],
  "cons": [
    { "title": "Минус 1", "description": "Описание минуса" },
    { "title": "Минус 2", "description": "Описание" }
  ]
}
```

### 🏆 Trust Badges (Факты)

В Power Pages поле `quickFacts` работает иначе. Оно отображается сразу под заголовком как **"иконки доверия"**.
_Используйте иконки формата `mdi:...` (Material Design Icons)._

**Пример JSON для `quickFacts`:**

```json
[
  { "title": "Гарантия", "value": "100%", "icon": "mdi:check-decagram" },
  { "title": "Студентов", "value": "500+", "icon": "mdi:account-group" },
  { "title": "Поддержка", "value": "24/7", "icon": "mdi:headset" }
]
```

---

## 💡 Сравнение режимов

| Фича                     | 📄 Обычная статья          | 🎓 Program (Направление) | ⚡ Power Page                 |
| ------------------------ | -------------------------- | ------------------------ | ----------------------------- |
| **URL**                  | `/blog/[slug]`             | `/program/[slug]`        | `/blog/[slug]`                |
| **Боковая колонка**      | ✅ Есть (Тэги, Поделиться) | ✅ Есть (Направления)    | ❌ Нет (Фуллскрин)            |
| **Оглавление**           | ✅ Автоматически           | ✅ Автоматически         | ❌ Нет                        |
| **Виджеты (ВУЗы, Цена)** | ✅ Да (`{{WIDGET}}`)       | ✅ Да (`{{WIDGET}}`)     | ✅ Да (`{{WIDGET}}`)          |
| **PROSCONS виджет**      | ✅ Да                      | ✅ Да                    | ✅ Да                         |
| **GALLERY виджет**       | ✅ Да                      | ✅ Да                    | ✅ Да                         |
| **Списки (Шаги, Чеки)**  | ✅ Работают                | ✅ Работают              | ✅ Работают                   |
| **FAQ (Аккордеон)**      | ✅ Работает                | ✅ Работает              | ✅ Работает                   |
| **Quick Facts**          | В сайдбаре (справа)        | В сайдбаре (справа)      | Под заголовком (Trust Badges) |
| **Флаг в seed**          | —                          | `isProgram: true`        | `isPowerPage: true`           |

---

## 📋 Пример полного JSON

Скопируй и поправь под себя:

```json
[
  {
    "type": "paragraph",
    "text": "Вступление. Турция — это круто."
  },
  {
    "type": "heading",
    "level": 2,
    "text": "Необходимые документы"
  },
  {
    "type": "list",
    "style": "checklist",
    "items": ["Паспорт", "Аттестат"]
  },
  {
    "type": "image",
    "url": "...",
    "width": "wide"
  },
  {
    "type": "heading",
    "level": 2,
    "text": "Как поступить?"
  },
  {
    "type": "list",
    "ordered": true,
    "style": "steps",
    "items": ["Заявка", "Переезд"]
  },
  {
    "type": "faq",
    "items": [{ "question": "Вопрос?", "answer": "Ответ." }]
  },
  {
    "type": "paragraph",
    "text": "Выбирайте лучшие вузы: {{WIDGET:UNIVERSITIES}}"
  }
]
```
