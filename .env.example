# Database Configuration
# You can use either DATABASE_URL (complete connection string) or individual components

# Option 1: Complete DATABASE_URL (recommended)
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Option 2: Individual components (if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=your_database_name

# When DATABASE_URL is not set, the application will construct it as:
# mysql://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME
