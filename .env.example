# Docker environment for database
DB_HOST=mysql
DB_PORT=3306
DB_ROOT_PASS=root_password_123
DB_APP_PASS=super_app_pass

# Prisma Database URLs (when running inside Docker containers, use host "mysql")
# Application connection (least privileges; used by Prisma Client and migrations)
DATABASE_URL=mysql://app_user:${DB_APP_PASS}@${DB_HOST}:${DB_PORT}/edu_turkish
# Shadow DB connection (privileged; used only by prisma migrate to create a temporary shadow database)
SHADOW_DATABASE_URL=mysql://root:${DB_ROOT_PASS}@${DB_HOST}:${DB_PORT}/mysql

# Application Configuration
APP_PORT=3000
NODE_ENV=development
NUXT_SECRET_KEY=

# Bitrix configurations
BITRIX_WEBHOOK_URL="https://eduturkish.bitrix24.com/rest/1/token/"

# Публичный идентификатор счётчика Яндекс.Метрики
NUXT_PUBLIC_YANDEX_METRIKA_ID=111111111

# Directus Configuration (Docker environment)
DIRECTUS_PUBLIC_URL=http://localhost:8055
DIRECTUS_ADMIN_EMAIL=zugeraev2@gmail.com
DIRECTUS_ADMIN_PASSWORD=Adam1970!!!
DIRECTUS_KEY=
DIRECTUS_SECRET=

# Caddy configurations
APP_DOMAIN=edu-turkish.com
DIRECTUS_DOMAIN=cms.edu-turkish.com
CADDY_ACME_EMAIL=admin@edu-turkish.com

# Tokens
OPENROUTER_API_KEY=
